dependencies:
  override:
    - echo "downloading dependencies"
test:
  override:
    - case $CIRCLE_NODE_INDEX in 0) echo "unit tests  package1" ;; 1) echo "unit tests  package1" ;; esac:
        parallel: true
deployment:
  production:
    branch: master
    commands:
      - echo "running deployment script"
