checkout:
  post:
    - touch checkoutfile
    - ls
dependencies:
  pre:
    - touch dep_pre
    - ls
  post:
    - touch dep_post
    - ls
test:
  override:
    - touch:
      parallel: true
      files:
        - 1t && ls
        - 2t && ls
        - 3t && ls
        - 4t && ls
deployment:
  production:
    branch: master
    commands:
      - ls
