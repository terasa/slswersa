checkout:
  post:
    - touch checkoutfile
    - ls
dependencies:
  pre:
    - touch dep_pre
    - ls
  post:
    - touch dep_post
    - ls
test:
  override:
    - case $CIRCLE_NODE_INDEX in 0) touch 1t && ls ;; 1) touch 2t && ls ;; 2) touch 3t && ls ;; 3) touch 4t && ls ;; 
      parallel: true
deployment:
  production:
    branch: master
    commands:
      - ls
