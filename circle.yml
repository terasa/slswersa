checkout:
  post:
    - touch checkoutfile
    - ls
dependencies:
  pre:
    - touch dep_pre
    - ls
  post:
    - touch dep_post
    - ls
test:
  override:
    - touch 1t && ls
    - touch 2t && ls
    - touch 3t && ls
    - touch 4t && ls
deployment:
  production:
    branch: master
    commands:
      - ls
